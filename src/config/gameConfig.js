export const GAME_WIDTH = 1200;
export const GAME_HEIGHT = 800;

// Other useful constants derived from game dimensions
export const CENTER_X = GAME_WIDTH / 2;
export const CENTER_Y = GAME_HEIGHT / 2;
